name: Run Shell Commands
on: [push, fork]

jobs:
  run-shell-commands:
    run-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "This is out first github action workflow file"